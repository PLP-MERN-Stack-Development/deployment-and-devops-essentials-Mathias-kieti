name: Backend CD

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install
        working-directory: ./server

      - name: Deploy to Render
        run: |
          echo "Deploy backend via Render dashboard or API"
          # Render auto-deploys when connected to GitHub repo
